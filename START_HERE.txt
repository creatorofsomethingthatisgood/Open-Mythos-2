
 
 MYTHOS LOCAL - START HERE 
 
 High-Quality Local LLM for AMD Hardware on Fedora 
 



 THE FIX IS COMPLETE - ALL MODEL URLs ARE NOW CORRECT 

The previous error was caused by incorrect HuggingFace model URLs.
This has been completely fixed. The config now uses bartowski's repos
which have the correct GGUF files.



 QUICK START (5 COMMANDS - COPY-PASTE ALL AT ONCE):


cd ~/mythos_local/amd-vulkan-llm-project
source venv/bin/activate
chmod +x *.sh
./show_config.sh
python3 main.py --mode chat



 WHAT EACH COMMAND DOES:


1. cd ~/mythos_local/amd-vulkan-llm-project
 → Navigate to project directory

2. source venv/bin/activate
 → Activate Python virtual environment
 → You'll see (venv) in your prompt

3. chmod +x *.sh
 → Make shell scripts executable

4. ./show_config.sh
 → Display current config and verify it's using bartowski
 → Should show " CORRECT - Using bartowski repos "

5. python3 main.py --mode chat
 → Start the chat interface
 → Will auto-download model on first run (~4.7GB, takes 5-10 min)
 → Then you can start chatting!



 ALTERNATIVE: FULL VERIFICATION FIRST


If you want to verify everything before starting:

cd ~/mythos_local/amd-vulkan-llm-project
source venv/bin/activate
chmod +x verify_setup.sh
./verify_setup.sh # Comprehensive check
python3 test_download.py # Test download only
python3 main.py --mode chat



 WEB INTERFACE (PRETTIER!)


For a web-based chat interface with sliders and controls:

source venv/bin/activate
python3 main.py --mode web

Then open: http://localhost:7860



 WHAT TO EXPECT


FIRST RUN:
• "No model found. Attempting auto-download..."
• Progress bar showing download (4.7GB)
• "Download complete! File size: 4.68 GB"
• "Loading model..." (takes ~15-20 seconds)
• Chat interface appears

SUBSEQUENT RUNS:
• Loads model from disk (~10-15 seconds)
• Ready to chat immediately

PERFORMANCE:
• CPU-only: 5-15 tokens/second
• With Vulkan: 15-30 tokens/second
• RAM usage: 6-10GB during inference



 CHAT COMMANDS (Terminal Mode)


/help → Show all commands
/clear → Clear conversation
/save → Save conversation
/load → Load previous conversation
/system creative → Use creative writing prompt
/system coding → Use coding assistant prompt
/temp 0.9 → Increase creativity
/reflect on → Enable quality self-reflection
/rag on → Enable document Q&A
/benchmark → Run quality tests
/quit → Exit



 IF YOU STILL SEE ERRORS


1. Verify you're in the right directory:
 pwd
 Should show: /home/a4/mythos_local/amd-vulkan-llm-project

2. Check config is correct:
 ./show_config.sh
 Should show: bartowski/Qwen2.5-7B-Instruct-GGUF

3. If config shows wrong repo:
 cat config.yaml | grep -A 2 "repo_id"
 Should show bartowski repos, not "Qwen/Qwen2.5..."

4. Clear Python cache:
 rm -rf __pycache__ engine/__pycache__ ui/__pycache__

5. Check for multiple config files:
 find ~/mythos_local -name "config.yaml"
 Should only show ONE file



 FILES THAT WERE FIXED/ADDED


FIXED:
 config.yaml → Now uses bartowski repos (VERIFIED 5x)
 engine/model_manager.py → Better logging to show which repo is used

ADDED:
 debug_config.py → Shows what config is actually loaded
 test_download.py → Test model download before running main app
 verify_setup.sh → Comprehensive setup verification
 show_config.sh → Quick config display
 START_HERE.txt → This file
 FIXED_README.txt → Detailed fix explanation



 READY TO START!


Copy-paste these 5 commands and you're done:

cd ~/mythos_local/amd-vulkan-llm-project
source venv/bin/activate
chmod +x *.sh
./show_config.sh
python3 main.py --mode chat


The model will auto-download on first run. Be patient!



Questions? The chat commands include:
• /help - see all available commands
• /benchmark - test model quality
• /system creative - switch to creative mode

Enjoy your Mythos-tier local LLM! 


