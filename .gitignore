# ─────────────────────── SECURITY ───────────────────────
# --- SSH keys ---
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
id_ecdsa
id_ecdsa.*
id_dsa
id_dsa.*
*.ppk
*.pub
known_hosts
authorized_keys

# --- GPG / PGP ---
*.gpg
*.asc
secring.*

# --- Key / certificate material ---
*.key
*.crt
*.cer
*.pfx
*.p12
*.jks
*.keystore
*.kdbx
*.p7b
*.p7c
*.der
*.csr
*.p8
*.pkcs12

# --- Cloud / platform credentials ---
.aws/
.gcloud/
.gcp/
.azure/
.kube/
kubeconfig
kubeconfig.*
.config/gcloud/
service-account*.json
*-service-account.json
gcloud-service-key.json
.oci/
.ibmcloud/
.docker/config.json

# --- Auth / token files ---
.env
.env.*
!.env.example
.netrc
.pypirc
.npmrc
.npmrc.local
.yarnrc.yml
.git-credentials
.gitconfig.local
.hf_token
huggingface_token*
*.hf_token
.openai_key
.anthropic_key
.npm_auth_token
.dockercfg

# --- Generic secret patterns ---
*secret*.json
*secrets*.yaml
*secrets*.yml
*.secrets
*credentials*.json
*credentials*.yaml
*credentials*.yml
*-credentials.*
token.txt
tokens.txt
auth.json
*.auth
*.token
*.apikey
*.api-key
*password*
*.passwd
*.htpasswd
.shadow

# --- Database dumps ---
*.dump
*.sql.dump
*.sql.gz
*.pcap
*.har

# ─────────────────────── BUILDS & CACHES ───────────────────────
# --- Python ---
__pycache__/
*.py[cod]
*.pyd
*.so
.Python
*.egg
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage/
coverage.xml
.tox/
.nox/
*.cover
.hypothesis/

# --- Virtual environments ---
venv/
.venv/
env/
ENV/
*-venv/
.*-venv/
venv-*/
.venv-*/
.mythos-venv/
.test-venv/
**/.test-venv/

# --- Aider ---
.aider*
.aider.tags.cache.v4/

# --- OpenCode ---
.opencode/
opencode.jsonc

# --- Python packages ---
*.egg-info/

# --- SQLite caches ---
*.db
*.db-shm
*.db-wal

# --- Node ---
node_modules/
.npm
.eslintcache
package-lock.json
pnpm-lock.yaml

# --- Git ---
.git/
.gitignore
.gitattributes

# --- Local LLM artifacts (download via setup scripts) ---
models/*.gguf
models/*.bin
models/*.safetensors
models/.cache/
!models/.gitkeep

# Hugging Face / sentence-transformers cache
.cache/

# RAG vector store (rebuilt with rag-index)
chroma_db/

# Portable copy of models/cache
offline-bundle/

# ─────────────────────── RUNTIME DATA ───────────────────────
mythos.log
*.log

# Saved chats
conversations/*.json
!conversations/.gitkeep

# Benchmark outputs
benchmarks/*
!benchmarks/.gitkeep

# LoRA training outputs
lora/*
!lora/.gitkeep

# User-provided RAG documents
rag_docs/*
!rag_docs/.gitkeep

# ─────────────────────── MYTHOS LOCAL STATE ───────────────────────
# In-tree Mythos runtime state (normally in ~/.config/mythos, but
# can end up here if someone sets MYTHOS_HOME=. or copies state for debugging)
sessions/
session_summaries/
rml_preferences.json
bitacora/
.mythos/
.mythos-venv/
mythos_home/
mythos.yaml
config.local.yaml
config.*.local.yaml
.mythos-test/

# ─────────────────────── AI / IDE / EDITOR STATE ───────────────────────
# These may contain prompts, transcripts, paths, or API keys
.openclaude/
.claude/
.cursor/
.copilot/
.windsurf/
.augment/
.aider*
.continue/
.history/
.zed/
.helix/
.nvim.lua
*.code-workspace

# ─────────────────────── SCRATCH / TEMP / LOCAL FILES ───────────────────────
# Dev/scratch files from AI coding sessions
CODING_ACTIVATED.txt
CODING_GUIDE.md
ELITE_CODING_MODE.tx
database.sql
t
FINAL_INSTRUCTIONS.txt
FINAL_SUMMARY.md
FIXED_README.txt
RUN_THIS.txt
CHECKLIST.txt
debug_config.py
test_download.py
show_download_url.py
*.tmp
*.bak
*.orig
*.rej
*.patch
*.patch.local
*.local
*.local.md
*.local.txt
*.scratch
*.prof
*.profraw
*.private
*.private.*
private/
PRIVATE/
scratch/
tmp/
_tmp/
.tmp/
TODO.local
TODO.local.*
NOTES.local
NOTES.local.*

# ─────────────────────── SHELL / OS HISTORY ───────────────────────
.bash_history
.zsh_history
.python_history
.lesshst
.viminfo
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
*.swp
*~
.idea/
.vscode/
*.sublime-*
.envrc.local

# ─────────────────────── AUDIT / SECURITY TOOLS ───────────────────────
# Local-only security audit scripts (not for distribution)
mythos_audit.py
mythos_bughunt.py

# ─────────────────────── HOST BUILD ARTIFACTS ───────────────────────
# Common build/install artifacts (when indexing external repos via RAG)
**/node_modules/
**/.next/
**/dist/
**/build/
**/coverage/
**/.vercel/
**/.pytest_cache/
**/.venv/
**/.ruff_cache/
.vercel
.nyc_output/

# ─────────────────────── VERCEL / DEPLOY ───────────────────────
.vercelignore
CLAUDE.md
.gitignore 
.dockerignore
.npmignore

#------------------------ Gemini's.md ---------------------------
.antigravitycli
GEMINI.md
